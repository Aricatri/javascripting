let total = 0;  // Cambiado a let, porque se va a modificar
const limit = 10;

for (let i = 0; i < limit; i++) {
  total += i;  // Acumula el valor de i en total
}

console.log(total);  // Imprime el total
